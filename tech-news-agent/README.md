Tech News Agent

Minimal scaffold for a news ingestion & scoring agent.

Usage:

- Create a `.env` from `.env.example` and set `FEED_URLS`.
- Install: `pip install -r requirements.txt`
- Run: `python main.py`
